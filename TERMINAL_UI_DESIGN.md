# ArgoFusion AFS v2.16.1 · 终端 UI / 文案设计稿

> 设计依据：`ArgoFusion_AFS_v2.14.12_TERMINAL_UI_DESIGN_v3.md`
>
> 对照脚本：`argofusion.sh`
>
> 适用版本：ArgoFusion · AFS v2.16.1

核心目标：**术语准确、文案等宽、布局工整、状态鲜明、交互精炼。** 本文是终端输出的唯一视觉合同；更新可见终端输出时，必须同步脚本、本文、README 与 `AGENTS.md`。

---

## 1. 设计方向

1. 主菜单中文功能名固定为四个汉字（八个显示宽度）；`BBR / DD` 与其视觉等宽。
2. 一级菜单仅说明功能，组件名称放在状态页、子菜单与结果页。
3. 使用实际术语：`Argo Tunnel`、`cloudflared`、`Sing-box`、`Xray`、`WARP`、`WS`、`XHTTP`、`WSS`、`SOCKS5`、`direct`、`AFS`。
4. 状态输出只回答：什么组件、当前什么状态、操作是否成功。
5. 正常导航静默返回；取消配置才明确说明配置没有变更。
6. 输入项统一为“对象 `[约束/默认值]`：”；确认项统一为“确认 + 动作？`[Y/n]`：”，支持大小写，直接回车表示确认，不使用“请输入”“是否”等冗余起句。

## 2. 视觉系统

固定 `UI_WIDTH=64`。分隔线为 64 个半角 `-`；显示宽度计算必须使用 `C.UTF-8`，避免未配置本机 locale 时将中文按字节误算。同一逻辑区不插空行，不同逻辑区最多一行空白。URL、节点 URI 与日志可自然换行。

| 语义 | ANSI | 用途 |
|---|---:|---|
| ASCII Logo / 键名 | `1;96` | 字标、键名、辅助标签 |
| 品牌 / 页面标题 / 输入 | `1;95` | `ArgoFusion`、`◆`、`›` |
| AFS 版本 / 菜单编号 / 导航 `0` / 警告 | `93` | 版本、编号、退出/返回/取消、停止或取消 |
| 分区 / 分隔线 | `1;94` | `▸`、64 列横线 |
| 正文 / URL | `97` | 普通值与白色下划线链接 |
| IP / 当前核心 | `95` | IP、优选入口、Core 名称 |
| 成功 | `92` | `✓`、运行中、校验通过 |
| 错误 | `91` | `✗`、失败、异常、回滚 |
| 处理中 | `96` | `• 正在…` |
| 次要说明 | `2;37` | 能力摘要与补充说明 |

图标的固定语义：`◆` 页面标题，`▸` 分区，`✓` 成功/正常，`!` 警告/停止/取消，`✗` 错误/失败/回滚，`•` 处理中/补充信息，`›` 用户输入。不得混用 `[OK]`、`[WARN]` 等前缀。

非 TTY、`TERM=dumb` 与设置 `NO_COLOR` 时，必须关闭所有 ANSI 控制符，而非删除文字、字标或排版。

## 3. 页面与提示模式

页面标题格式固定为 `◆ ArgoFusion · <页面名称>`。提示模式由 `ui_page()` 统一提供：

```text
main    0 · 退出
back    0 · 返回
cancel  0 · 取消
default Enter · 默认 | 0 · 取消
none    不显示提示。
```

主面板使用 `main`，菜单使用 `back`，确认使用 `cancel`，有默认值或现有值的表单统一使用 `default`，只读页和执行结果使用 `none`。提示固定在页面标题同一行右端：`Enter`、`0` 使用亮黄，说明、竖线与分隔点使用亮白；标题过长时自动换到下一行，避免截断。输入项只展示约束、现有值或默认值，不再逐项附加“按 Enter 保持”；直接回车统一采用方括号中的值。普通返回不输出提示；配置表单取消只输出：

```text
! 已取消修改，配置未变更。
```

## 4. 首页与主菜单

首页依次显示 ArgoFusion 斜体字标、摘要、系统环境、64 列横线、运行状态，再显示控制中心和菜单。状态顺序固定：

```text
Argo Tunnel
代理核心
WARP 分流
节点概览
Argo 域名
优选入口
Argo 回源
组件版本
```

节点概览固定显示 `Vless n · Vmess n · Trojan n · XHTTP n · SS n`，不显示总数。

```text
◆ ArgoFusion · 控制中心                              0 · 退出
----------------------------------------------------------------
▸ 日常管理
   1  节点订阅                    [af -n]
   2  服务启停                    [af -a]
   3  核心切换                    [af -p]
   4  参数配置                    [af -c]
   5  流量统计                    [af -t]
   6  运行诊断                    [af -x]

▸ 系统维护
   7  项目安装                    [af -i]
   8  组件更新                    [af -v]
   9  备份恢复                    [af -k]
  10  BBR / DD                    [af -b]
  11  项目卸载                    [af -u]
   0  退出脚本
----------------------------------------------------------------
› 请选择：
```

`menu_item()` 的编号为两位右对齐，功能列用显示宽度补至 28，快捷命令从固定列开始。不得为四字主菜单添加解释性长句。

## 5. 标准页面

### 节点订阅

页面名为 `节点订阅`。订阅链接按以下次序输出：订阅面板、自适应订阅、原始节点订阅、Base64 订阅、Clash/Mihomo 订阅、Sing-box 订阅。标签按固定显示宽度对齐，所有 URL 为亮白下划线；自动适配 QR 唯一保留，不为每个节点或其它订阅重复输出。网页订阅中心保持白底蓝字；指定格式卡片固定按“原始节点订阅、Base64、Clash/Mihomo、Sing-box”排列。

节点标题使用：

```text
[01] Argo-Vl · Vless+WS+TLS
[02] Argo-Vm · Vmess+WS+TLS
[03] Argo-Tr · Trojan+WS+TLS
[04] Argo-Xh · Vless+XHTTP+TLS
[05] Argo-Sh · Shadowsocks+WS+TLS
```

`af -n` 的 `原始节点` 块显示编号、亮紫标签、完整协议/传输/TLS 类型和节点 URI，不显示传输路径或出站信息。终端协议统一显示为 `Vless`、`Vmess`、`Trojan`、`XHTTP`、`SS`；标题中的完整类型按上例显示。参数配置中的节点表固定使用 `13 / 6 / 14 / 5 / 18` 显示宽度，总宽度不得超过 64 列；过长字段以 `~` 截断，路径列标题为“传输路径”。协议使用亮蓝，直连与 SOCKS5 出站 IP/端口统一使用亮紫，不得暴露用户名或密码。仅从参数配置选择“查看节点”时在“节点列表”前留一行，添加、修改和删除节点页面不额外插入空行。

### 服务启停

```text
◆ ArgoFusion · 服务启停                                  0 · 返回
----------------------------------------------------------------
▸ 服务状态
Argo Tunnel    运行中
代理核心       运行中 · Xray

▸ 服务操作
   1  Argo 启停
   2  核心启停
   3  重启服务
   0  返回上级
```

处理文案统一为“正在 + 动作 + 对象”，例如 `• 正在启动 Argo Tunnel...`、`✓ Argo Tunnel 已启动。`、`! Xray Core 已停止。`。检查结果使用“`<对象> 已通过。` / `<对象> 无效。` / `<对象> 未运行。`”，不只输出孤立的组件名称。

### 核心配置

主菜单仍使用“核心切换”，页面名为 `核心配置`，使用 `back` 提示。代理状态、共享范围与保留文件合并为“核心配置”单一分区，依次显示“运行中 · Sing-box / Xray”、环境配置/节点定义/Nginx/订阅和 `sing-box.json`/`xray.json`。选项为 `Sing-box`、`Xray`、`返回上级`。切换输出统一为 `• 正在切换代理核心...` 和 `✓ 代理核心已切换：<旧核心> → <新核心>。`；操作结束后继续停留在核心配置页，输入 `0` 才返回主面板。切换只重启并验证 Nginx 与目标代理核心，不应由 Argo Tunnel 的无关瞬时状态导致回滚；失败必须先说明配置验证失败、展示失败服务状态与最近日志，再说明已恢复的原核心。

### 流量统计

页面名为 `流量统计`，使用 `back` 提示。固定按“统计状态 → 入站统计 → 出站统计 → 统计操作”排列。状态显示每分钟定时采集、统计起点和最近采集时间；核心计数暂不可读时保留并展示 SQLite 中的历史值，同时输出黄色提示。入站和出站表均使用“标签 / 上传 / 下载 / 合计”四列，节点标签使用亮紫，数值使用亮白，单位按 B、KB、MB、GB、TB 自动换算。

入站表按 `nodes.conf` 顺序显示当前全部节点；已删除但仍有累计值的标签另列“历史节点”，直到用户重置。出站表按 direct、warp、节点 SOCKS5 的顺序显示已有数据。操作固定为“刷新统计、重置统计、返回上级”。重置必须使用 `确认清空全部历史流量？[Y/n]：`，取消不修改数据，成功输出 `✓ 流量统计已重置。`。

### 参数配置与 WARP

参数配置使用三个分区：`Argo 配置`（Token 域名、优选入口、回源端口、全局 UUID）、`节点配置`（查看、添加、修改、删除）和 `WARP 配置`（WARP 分流）。

WARP 页面名为 `WARP 分流`，主面板状态键固定为 `WARP 分流`，并显示代理端口、目标域名。菜单项为 `启用配置`、`添加域名`、`删除域名`、`停用 WARP`。状态使用：`未启用`、`运行中 · 127.0.0.1:端口`、`代理异常`；前两者分别为黄色、绿色，异常为红色。本机 WARP 地址使用亮白；仅公网 IP 与优选入口 IP 使用亮紫。添加或删除域名时直接回车不执行修改、不报错，并返回 WARP 操作页。

配置成功统一为：

```text
• 正在应用配置...
✓ 配置已生效。
```

配置失败必须说明正在回滚、已恢复原配置及配置未生效。

### 服务启停、诊断与安装

- 所有交互子页面均按“状态或说明 → 操作”排列：先使用亮蓝分区显示当前状态、配置范围、备份配置或风险提示，再显示菜单或确认输入。菜单与确认页在输入前以亮蓝分隔线收束；服务重启选择后直接执行，重启范围与处理提示之间不另加横线。
- `服务启停` 的 `重启服务` 操作显示“重启范围”分区及 `重启服务  Nginx · <Core> · Argo Tunnel`，范围信息后不再输出额外横线；选择后先执行 `systemctl daemon-reload`，再直接使用 `• 正在重启全部服务...` 与 `✓ 全部服务已重启。`，不再二次确认。
- `运行诊断` 分为 `系统与组件状态`、`配置检查`、`服务检查`、`WS 检查`、`诊断结果`；组件版本位于首个概览分区。Xray 成功配置检查必须静默，失败时过滤配置读取行以及 WebSocket、VMess、Trojan 弃用提醒，只保留真实错误；诊断日志采用相同过滤规则。
- `项目安装` 提供 `本地重装` 与 `在线更新`；在线项下用次要说明提示“会先校验并替换本地脚本”。完整安装只在起始页显示字标，随后用环境检查、组件安装、配置部署、服务启动和运行检查分区汇报。

### 组件更新、备份恢复、BBR / DD 与项目卸载

- `组件更新` 用准确名称 `cloudflared` 与当前 `Sing-box` / `Xray`，不称“Argo 核心”。
- `备份恢复` 只操作节点配置，菜单项为 `节点备份`、`节点恢复`；恢复失败必须自动回滚。
- `BBR / DD` 是第三方 `Linux-NetSpeed` 的确认页；以“风险提示”分区说明其可能修改 Linux 内核、BBR 与系统磁盘。
- `项目卸载` 使用 `cancel` 提示，并以“将移除”分区列出 AFS systemd 服务、Sing-box/Xray/cloudflared、`/etc/afs` 配置与订阅及 `af / AF` 入口；可选卸载项仍须逐项确认。

## 6. 术语与状态文案

统一使用：`Argo Tunnel`、`cloudflared`、`Sing-box`、`Xray`、`WARP`、`Nginx`、`WS`、`XHTTP`、`WSS`、`SOCKS5`、`direct`、`AFS`、`Argo Token`、`Argo 域名`、`Argo 回源`、`优选入口`、`节点`、`订阅`。

```text
Cloudflare Tunnel / Argo Tunnel  → Argo Tunnel
warp / Warp / WARP               → WARP
socks5 / Socks5 / SOCKS5         → SOCKS5
直连 / Direct / direct           → direct
WebSocket / websocket / WS       → WS
```

成功、停止/取消、错误和处理中分别使用以下句式：

```text
✓ <对象> 已启动。        ! <对象> 已停止。        ✗ <对象> 启动失败。
✓ <对象> 已更新。        ! <对象> 未启用。        ✗ <对象> 配置无效。
✓ <对象> 校验通过。      ! 已取消操作。          ✗ <对象> 校验失败。
• 正在启动 <对象>...     • 正在更新 <对象>...    • 正在应用配置...
```

输入提示示例：

```text
› 节点协议 [vless/vmess/trojan/vless-xhttp/shadowsocks]：
› 优选入口 [example.com:443]：
› 确认卸载 ArgoFusion？[Y/n]：
```

已有配置项必须把当前值写入提示，页面标题右端统一显示 `Enter · 默认 | 0 · 取消`；不再区分“保持”和“默认”，也不在单项输入中重复回车说明。新安装的必填项仍拒绝留空。节点添加、修改和删除必须逐项校验，输入错误时只重试当前步骤，不退出脚本。

## 7. 验收

1. `bash -n argofusion.sh` 通过，脚本为 LF 行尾。
2. 主菜单的 12 个项目（含退出）与本文完全一致，快捷命令保持不变。
3. 颜色关闭环境不含 ANSI 控制符，仍能阅读标题、状态、菜单与输入提示。
4. `af -n` 的协议大小写、订阅标签、唯一自动适配 QR 与节点表列宽符合本文。
5. `af -x` 检查 `config/data` 的 `700`、`subscriptions` 的 `755`；健康时不展开日志。
6. 涉及服务、Nginx、Cloudflare 或公网 WS/XHTTP 的改动仍须在真实 Debian/Ubuntu VPS 上进行运行时验证；静态检查不构成端到端证明。
